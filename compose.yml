services:
  spflat:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app/
    command: python -m sender_policy_flattener -c /app/spfs.json
    working_dir: /app
    env_file:
      - .env